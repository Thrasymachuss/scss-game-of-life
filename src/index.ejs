<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        Game Of Life In SCSS
    </title>
</head>

<body>
    <header class="header">
        <h1>Game Of Life</h1>
        <p>An implementation of Conway's Game of Life written in SCSS. Source code and a longer description of the
            project
            can
            be found <a href="https://github.com/Thrasymachuss/scss-game-of-life">here</a>.</p>
    </header>
    <div class="board-wrap">
        <div class="board">
            <% for (var i=1; i <=20; i++ ) { %>
                <div class="row">
                    <% for (var j=1; j <=20; j++ ) { %>
                        <div class="cell cell-<%= i %>-<%= j %>">
                            <div class="cell-inner"></div>
                        </div>
                        <% } %>
                </div>
                <% } %>
        </div>
    </div>
</body>

</html>